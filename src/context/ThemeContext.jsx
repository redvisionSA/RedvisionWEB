/**
 * OBSOLETO — conservado solo por compatibilidad.
 * El estado de la interfaz vive ahora en SiteContext.jsx (tema + modo
 * vigilancia + deteccion de puntero). Importa desde ahi en codigo nuevo.
 */
export { SiteContext as ThemeContext, useSite, useTheme } from './SiteContext.jsx'
